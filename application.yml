spring:
  application:
    name: greeting-service
server:
  port: 8081
greeting-service:
  greeting: "Hello"
  default-value: "World"
management:
  endpoints:
    web:
      exposure:
        include: "*"
        #- 'mappings'
        #- 'refresh'